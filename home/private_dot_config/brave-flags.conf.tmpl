# Read by /usr/bin/brave. Pins the Interceptor build on every launch: without it
# Brave kept serving the old developer-mode worker after an extension rebuild.
--load-extension={{ .chezmoi.homeDir }}/code/repos/Interceptor/extension/dist
