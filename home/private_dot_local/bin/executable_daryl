#!/bin/sh
# daryl — launch the primary PAI Daryl (Claude Code, full PAI context).
# Friendly alias for the `pai` launcher (credentials + system prompt + MCP).
# The goose-based Daryl variants are gd / gd-gpt / gd-gemini.
exec pai "$@"
