#!/bin/sh
# gd — goose Daryl on the default provider (Claude via claude-acp).
# Usage: gd [dir]
if [ -r "$HOME/.config/daryl/mail-env.sh" ]; then . "$HOME/.config/daryl/mail-env.sh"; fi
exec goose-daryl "$@"
