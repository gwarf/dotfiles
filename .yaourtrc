#    yaourtrc  - Configuration for yaourt
#
#   See yaourtrc(5) for more information

# General
AUTOSAVEBACKUPFILE=1
DEVELSRCDIR="/var/abs/local/yaourtsrc"
DEVELBUILDDIR="/var/abs/local/yaourtbuild"
DEVEL=1
EDITOR="vim"
#FORCEENGLISH=0
#FORCE=0
#TMPDIR="/var/abs/local/yaourt-tmp"

# SUDO
#SUDONOVERIF=0      # Avoid multiple sudo checks when timestamp_timeout=0
#SUDOREDIRECT=1     # Define to 0 in case you use a fingerprint device

# ABS
# If the package "abs" is installed, those var are parsed from abs.conf
#REPOS=()           # REPOS available at $SYNCSERVER
#SYNCSERVER=""

# AUR
#AURURL="https://aur.archlinux.org"
#AURCOMMENT=5
#AURDEVELONLY=0
#AURSEARCH=1
AURUPGRADE=1
AURVOTE=1

# Build
EXPORT=1           # Export to 1: EXPORTDIR or PKGDEST
                    # 2: pacman cache (as root)
EXPORTSRC=1        # Need EXPORT>0 to be used
EXPORTDIR="/var/abs/local/exports"       # If empty, use makepkg's connfiguration (see makepkg.conf)

# Prompt
#NOCONFIRM=0
UP_NOCONFIRM=1     # No prompt while build upgrades (including -Sbu)
BUILD_NOCONFIRM=1  # Only prompt for editing files
#PU_NOCONFIRM=0     # Add --noconfirm to $PACMAN -U
EDITFILES=0
#NOENTER=1

# Output
#USECOLOR=1
#USEPAGER=0
DETAILUPGRADE=4
SHOWORPHANS=0
#TERMINALTITLE=1

# Command
#PACMAN="pacman"
DIFFEDITCMD="vimdiff"
