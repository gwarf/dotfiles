- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.vim: vim
    ~/.config/neomutt: config/neomutt
    ~/.yaourtrc: yaourtrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
